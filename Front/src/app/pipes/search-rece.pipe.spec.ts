import { SearchRecePipe } from './search-rece.pipe';

describe('SearchRecePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRecePipe();
    expect(pipe).toBeTruthy();
  });
});
