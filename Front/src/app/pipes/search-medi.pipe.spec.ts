import { SearchMediPipe } from './search-medi.pipe';

describe('SearchMediPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMediPipe();
    expect(pipe).toBeTruthy();
  });
});
